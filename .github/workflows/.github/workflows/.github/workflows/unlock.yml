name: 🎄 Unlock Christmas Gift

on:
  schedule:
    - cron: "0 5 * * *"   # 12:00 trưa VN = 05:00 UTC
  workflow_dispatch:

jobs:
  unlock:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Replace index with gift
        run: |
          mv gift.html index.html

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add index.html
          git commit -m "🎄 Unlock Christmas gift"
          git push
